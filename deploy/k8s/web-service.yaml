apiVersion: v1
kind: Service
metadata:
  name: serverhub-web
  namespace: serverhub
  labels:
    app: serverhub-web
spec:
  type: ClusterIP
  selector:
    app: serverhub-web
  ports:
    - name: http
      port: 3000
      targetPort: 3000
