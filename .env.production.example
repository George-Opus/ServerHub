# Copier ce fichier en .env sur le serveur avant docker compose up
# Générer SECRET_KEY : python -c "import secrets; print(secrets.token_urlsafe(48))"

SECRET_KEY=change-me-generate-a-random-secret
CORS_ORIGINS=http://VOTRE_IP:3000
NEXT_PUBLIC_API_URL=http://VOTRE_IP:8000
